import { MainLayout } from 'src/components/layout/main';
import { HomePageContainer } from 'src/containers/HomePage';

const HomePage = () => {
	return <HomePageContainer />;
};

HomePage.Layout = MainLayout;

export default HomePage;
